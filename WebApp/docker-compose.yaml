services:
  db:
    container_name: postgres-webapp
    image: postgres:latest
    environment:
      POSTGRES_USER: aeon
      POSTGRES_PASSWORD: 152346
      PGDATA: /data/postgres
    volumes:
      - db:/data/postgres
    ports:
      - "5332:5432"
    networks:
      - db
    restart: unless-stopped

networks:
  db:
    driver: bridge

volumes:
  db: